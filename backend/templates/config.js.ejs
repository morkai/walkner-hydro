window.LOCALE = 'pl';
localStorage.setItem('LOCALE', 'pl');
appLocale = (localStorage.getItem('LOCALE') || (navigator.languages ? navigator.languages[0] : navigator.language) || window.LOCALE || 'pl').split('-')[0];
appLocale = 'pl';//appLocale === 'pl' ? 'pl' : 'en';
require = {
  baseUrl: '/',
  paths: <%- paths %>,
  shim: <%- shim %>,
  config: {
    i18n: {
      locale: appLocale
    }
  }
};

if (typeof window === 'object' && location.hostname === 'hydro.walkner.pl')
{
  require.baseUrl = location.protocol
    + '//s.walkner.pl:'
    + (location.protocol === 'https:' ? 443 : 80)
    + '/hydro.walkner.pl/';
}
