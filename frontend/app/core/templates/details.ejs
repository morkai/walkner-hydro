<div class="well">
  <dl>
    <% _.each(data, function(value, name) { %>
    <dt><%= name %>
    <% if (_.isArray(value)) { %>
    <% _.each(value, function(entry) { %>
    <dd><%- entry %>
    <% }) %>
    <% } else { %>
    <dd><%= value %>
    <% } %>
    <% }) %>
  </dl>
</div>
